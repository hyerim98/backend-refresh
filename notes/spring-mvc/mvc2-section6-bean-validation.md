# Section 06 - Bean Validation

> Bean Validation은 애노테이션 기반으로 입력 값을 자동 검증하는 기술이다.

---

# 1️⃣ Bean Validation 개념

- 자바 표준 검증 기술 (JSR-380)
- 애노테이션 기반 검증 지원
- 스프링 MVC와 자동 통합됨

기존 방식:
- if문 검증
- rejectValue 직접 호출

Bean Validation:
- 애노테이션 선언만으로 검증 수행

---

# 2️⃣ 주요 검증 애노테이션

## 문자열 검증

@NotNull
- null 허용하지 않음

@NotEmpty
- null + "" 허용하지 않음

@NotBlank
- null + "" + 공백 허용하지 않음

@Size
- 문자열 길이 제한

---

## 숫자 검증

@Min
- 최소값 제한

@Max
- 최대값 제한

@Range
- 범위 제한

---

# 3️⃣ @Validated 역할 ⭐

컨트롤러에서 검증을 실행시키는 트리거 역할

@Validated가 존재해야:

- Bean Validation 실행
- 검증 결과 생성
- BindingResult에 오류 저장

---

# 4️⃣ Bean Validation 동작 흐름

클라이언트 요청  
↓  
@ModelAttribute 객체 생성  
↓  
데이터 바인딩  
↓  
@Validated 확인  
↓  
Bean Validation 수행  
↓  
검증 실패 시 오류 생성  
↓  
BindingResult 저장  
↓  
View 반환

---

# 5️⃣ Validation 구조와의 관계

Bean Validation은 기존 Validation 구조 위에서 동작한다.

즉,

✔ BindingResult 사용  
✔ 오류 메시지 처리 동일  
✔ View 오류 출력 동일

단지 검증 로직이 자동화됨

---

# 6️⃣ 메시지 처리

검증 애노테이션은 메시지 코드 생성

예:

NotBlank.itemName  
Range.price

→ messages.properties에서 메시지 조회

---

# 7️⃣ Bean Validation의 한계

단일 필드 검증에는 적합하지만

다음은 처리 어려움:

- 필드 간 비교
- 복합 비즈니스 로직

→ 직접 Validation 필요

---

# 🔥 핵심 정리

Bean Validation은

검증 로직을 애노테이션으로 선언하고  
스프링이 자동으로 검증을 수행하도록 만드는 기술이다.