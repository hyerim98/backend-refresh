# 섹션 8. 의존관계 자동 주입 (Autowired)

## 1. 자동 주입이 필요한 이유
- 설정(AppConfig) 코드가 계속 증가함
- 구현 변경 시 설정 코드 수정 필요
- 객체 생성과 의존관계 연결 책임을 스프링 컨테이너로 위임

## 2. 의존관계 주입 방법 정리
- 생성자 주입 (권장)
- 수정자(setter) 주입
- 필드 주입 (권장하지 않음)

## 3. 생성자 주입을 권장하는 이유
- 객체 생성 시점에 의존관계가 확정됨
- 필수 의존성 누락 방지
- 불변 객체 설계 가능
- 테스트 시 스프링 없이도 객체 생성 가능

## 4. @Autowired 기본 동작 원리
- 기본 전략은 타입(Type) 기준 조회
- 타입에 맞는 빈이 하나면 바로 주입
- 생성자가 1개면 @Autowired 생략 가능

## 5. 같은 타입의 빈이 여러 개일 때 문제
- 타입 기준으로 하나를 선택할 수 없음
- 스프링이 주입 대상 결정을 못함
- 발생 예외:
    - NoUniqueBeanDefinitionException

## 6. 해결 방법 1: @Qualifier
- 주입 시 빈 이름을 명시적으로 지정
- 정확한 구현체 선택 가능
- 이름 기반 → 오타에 취약

## 7. 해결 방법 2: @Primary
- 여러 빈 중 기본으로 선택될 빈 지정
- 별도 지정 없으면 @Primary 빈이 우선 주입됨
- 메인 구현체에 적합
- 👉 **실습 코드**: practice/spring/spring-core-basic/src/main/java/hello/core/section8_autowired/discount/RateDiscountPolicyImpl

## 8. @Qualifier vs @Primary
- @Qualifier: 명확한 선택
- @Primary: 기본값 제공
- 우선순위: @Qualifier > @Primary
- 많이 사용: @Qualifier < @Primary

## 9. 모든 빈 조회
- 같은 타입의 빈을 모두 주입 가능
- Map 또는 List 형태로 조회
- 전략 패턴 구현 시 유용
- 👉 **실습 코드**: practice/spring/spring-core-basic/src/test/java/hello/core/section8_autowired/AllBeanTest

## 10. 필드 주입을 지양해야 하는 이유
- 테스트가 어려움
- DI 컨테이너 의존성 증가
- 객체 불변성 유지 불가

## 11. 자동 주입 정리
- 생성자 주입이 기본 선택
- @Autowired는 타입 기준 동작
- 중복 타입 발생 시 @Qualifier 또는 @Primary 사용
- 설정 코드 최소화 + 구조 안정성 확보